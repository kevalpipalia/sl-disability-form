function downloadPDF() {
    // Call the backend service to generate and download the PDF
    window.location.href = '/generate_pdf';
}
function advanceGeneration() {
    // Call the backend service to generate and download the PDF
    window.location.href = '/advance_generation';
}
